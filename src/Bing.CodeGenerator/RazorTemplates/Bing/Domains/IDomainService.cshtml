@model BuildContext
@{
    var project = Model.Project;
    var buildTask = Model.Build;
    var schema = Model.GetCurrentSchema();
    var table = Model.GetCurrentTable();
    var entity = Model.GetEntityContext().FindEntity(schema, table);
}
using Bing.Domains.Services;

namespace @(Model.GetDomainName("Services","Abstractions"))
{
    /// <summary>
    /// @(entity.Description)管理
    /// </summary>
    public interface I@(entity.ClassName)Manager : IDomainService
    {
    }
}